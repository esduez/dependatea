name: dependatea
version: 1.0.0
description: "tea.xyz entegre bağımlılık analiz ve token ödül CLI aracı"
dependencies:
  - requests     # API istekleri için
  - matplotlib   # Grafik oluşturma
  - networkx     # Bağımlılık ağ analizi
  - pytest       # Testler için (opsiyonel)